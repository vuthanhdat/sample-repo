# Tasks

Task là execution contract, không chỉ là TODO title. Mỗi task phải có objective, read set, write scope, dependencies, acceptance, verification và result/evidence expectations.

Task có thể được giao cho Human hoặc Machine Principal nhưng schema/lifecycle không đổi.