# Environment Operations

Chứa operational differences giữa dev/test/staging/production: endpoints, external dependencies, configuration ownership, secret references, data policy và environment-specific constraints.

Environment topology/design thuộc Infrastructure Design; folder này giữ thông tin phục vụ vận hành thực tế.